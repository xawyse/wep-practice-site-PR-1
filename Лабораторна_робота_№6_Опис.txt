═══════════════════════════════════════════════════════════════════════════════
ЛАБОРАТОРНА РОБОТА №6: АНІМАЦІЯ ПОКЕМОНІВ ЗІ СПРАЙТАМИ
═══════════════════════════════════════════════════════════════════════════════

📋 ВИМОГИ:
───────────────────────────────────────────────────────────────────────────────
1. Анімація спрайта (рух, підстрибування, масштабування)
2. Анімація фону (переміщення хмар, паралакс)
3. Чистий CSS для анімації (@keyframes)
4. JavaScript тільки для випадкових параметрів

═══════════════════════════════════════════════════════════════════════════════
🎬 АНІМАЦІЯ ФОНУ (НЕБО)
═══════════════════════════════════════════════════════════════════════════════

```css
.scene__sky {
    background: linear-gradient(120deg, #0b1b51, #1b4ab8, #32a4ff);
    background-size: 300% 300%;      /* Градієнт більший за екран */
    animation: sky-move 12s linear infinite;
}

@keyframes sky-move {
    0% { background-position: 0% 50%; }
    100% { background-position: 100% 50%; }
}
```

Пояснення:
- `background-size: 300% 300%` - градієнт в 3 рази більший за екран
- `animation` - застосовує анімацію `sky-move` тривалістю 12 секунд
- `linear` - рівномірна швидкість
- `infinite` - безкінечне повторення
- `@keyframes` - визначає кадри анімації (0% = початок, 100% = кінець)
- `background-position` - змінює позицію градієнта, створюючи ефект руху

═══════════════════════════════════════════════════════════════════════════════
🎮 АНІМАЦІЯ ПОКЕМОНІВ
═══════════════════════════════════════════════════════════════════════════════

```css
.pokemon {
    animation-name: pokemon-move, pokemon-bob;  /* Дві анімації одночасно */
    animation-iteration-count: infinite, infinite;
    animation-timing-function: linear, ease-in-out;
}

@keyframes pokemon-move {
    0% { left: -120px; }              /* Починається зліва за екраном */
    100% { left: 100%; }              /* Закінчується справа за екраном */
}

@keyframes pokemon-bob {
    0%, 100% { transform: translateY(0) scale(1); }
    50% { transform: translateY(-20px) scale(1.1); }  /* Підстрибування + збільшення */
}
```

Пояснення:
- `animation-name` - дві анімації: рух по X і підстрибування
- `pokemon-move` - рух зліва направо (left: -120px → 100%)
- `pokemon-bob` - вертикальне підстрибування + масштабування
- `transform: translateY(-20px) scale(1.1)` - зсув вгору на 20px + збільшення на 10%

═══════════════════════════════════════════════════════════════════════════════
⚙️ JAVASCRIPT ДЛЯ ВИПАДКОВИХ ПАРАМЕТРІВ
═══════════════════════════════════════════════════════════════════════════════

```javascript
pokemons.forEach((el) => {
    // Випадкова висота
    const top = 10 + Math.random() * 50;  // 10% - 60%
    el.style.top = `${top}%`;
    
    // Випадкова швидкість
    const moveDuration = 5 + Math.random() * 5;  // 5-10 секунд
    const bobDuration = 2 + Math.random() * 2;     // 2-4 секунди
    el.style.animationDuration = `${moveDuration}s, ${bobDuration}s`;
    
    // Випадкова затримка
    const delay = Math.random() * 5;  // 0-5 секунд
    el.style.animationDelay = `${delay}s, 0s`;
});
```

Пояснення:
- `Math.random()` - випадкове число від 0 до 1
- `Math.random() * 50` - випадкове число від 0 до 50
- `10 + Math.random() * 50` - від 10 до 60
- Кожен покемон має унікальні параметри: висота, швидкість, затримка

═══════════════════════════════════════════════════════════════════════════════

