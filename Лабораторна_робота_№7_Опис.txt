═══════════════════════════════════════════════════════════════════════════════
ЛАБОРАТОРНА РОБОТА №7: НОВОРІЧНА ЛИСТІВКА (BEM + SCSS)
═══════════════════════════════════════════════════════════════════════════════

📋 ВИМОГИ:
───────────────────────────────────────────────────────────────────────────────
1. Використати методологію BEM (Block Element Modifier)
2. Використати препроцесор SCSS
3. Винести основні значення в змінні
4. Ізоляція в окремій директорії

═══════════════════════════════════════════════════════════════════════════════
🏗️ BEM МЕТОДОЛОГІЯ
═══════════════════════════════════════════════════════════════════════════════

BEM = Block__Element--Modifier

Приклад:
```html
<header class="header">
    <div class="header__container">
        <img class="header__logo" src="...">
        <div class="header__text">
            <p class="header__subtitle">...</p>
            <h1 class="header__title">...</h1>
        </div>
    </div>
</header>
```

Пояснення:
- `header` - блок (Block)
- `header__container` - елемент блоку (Element)
- `header__logo` - елемент блоку
- `header__text` - елемент блоку
- `header__subtitle` - елемент блоку
- `header__title` - елемент блоку

Модифікатор (якщо потрібен):
```html
<button class="button button--primary">...</button>
<button class="button button--secondary">...</button>
```

═══════════════════════════════════════════════════════════════════════════════
📝 SCSS ЗМІННІ
═══════════════════════════════════════════════════════════════════════════════

```scss
$bg-main: radial-gradient(circle at top, #1f3c73, #050816 65%);
$text-color: #f7f7ff;
$card-bg: rgba(5, 8, 22, 0.85);
$border-color: rgba(255, 255, 255, 0.12);
$font-main: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
$container-width: 960px;
```

Використання:
```scss
body.page {
    background: $bg-main;
    color: $text-color;
    font-family: $font-main;
}
```

Переваги:
- Легко змінити колір в одному місці
- Консистентність по всьому сайту
- Просте темування

═══════════════════════════════════════════════════════════════════════════════
🔧 SCSS ВКЛАДЕНІСТЬ (NESTING)
═══════════════════════════════════════════════════════════════════════════════

```scss
.header {
    padding: 16px 0;
    
    &__container {              /* .header__container */
        max-width: $container-width;
        margin: 0 auto;
    }
    
    &__logo {                   /* .header__logo */
        width: 64px;
        height: 64px;
    }
    
    &__text {                   /* .header__text */
        display: flex;
        flex-direction: column;
    }
}
```

Пояснення:
- `&` - посилання на батьківський селектор
- `&__container` компілюється в `.header__container`
- Вкладеність відповідає структурі HTML
- Легше читати і підтримувати

═══════════════════════════════════════════════════════════════════════════════
📦 СТРУКТУРА ПРОЄКТУ
═══════════════════════════════════════════════════════════════════════════════

```
lab7/
├── index.html
├── css/
│   ├── nullstyle.css      (CSS reset)
│   └── index.css           (скомпільований CSS)
└── sources/
    └── index.scss          (SCSS джерело)
```

Процес:
1. Пишемо SCSS в `sources/index.scss`
2. Компілюємо в `css/index.css`
3. Підключаємо `css/index.css` в HTML

═══════════════════════════════════════════════════════════════════════════════

